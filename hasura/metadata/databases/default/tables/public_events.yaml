table:
  name: events
  schema: public
array_relationships:
  - name: event_paticipants
    using:
      foreign_key_constraint_on:
        column: event_id
        table:
          name: event_paticipants
          schema: public
  - name: images
    using:
      foreign_key_constraint_on:
        column: event_id
        table:
          name: images
          schema: public
