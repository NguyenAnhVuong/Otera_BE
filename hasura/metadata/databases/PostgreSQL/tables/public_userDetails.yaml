table:
  name: userDetails
  schema: public
object_relationships:
  - name: deceased
    using:
      foreign_key_constraint_on:
        column: userDetailId
        table:
          name: deceaseds
          schema: public
  - name: user
    using:
      foreign_key_constraint_on:
        column: userDetailId
        table:
          name: users
          schema: public
